export enum AppState {
  Close = 'close',
  Error = 'error',
  Loading = 'loading',
  Mounted = 'mounted',
  Ready = 'ready'
}

export enum LanguageCode {
  EN = 'en',
  ES = 'es'
}

export enum EnvVariable {
  BaseUrl = 'REACT_APP__BASE_URL',
  ParentTargetOrigin = 'REACT_APP__PARENT_TARGET_ORIGIN',
}
